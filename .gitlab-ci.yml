bounds:
  stage: test
  image: python:3.12-slim
  rules:
  - changes:
    - src/**/*
    - .bounds/**/*
  script:
  - pip install bounds-cli
  - bounds calibrate --check || true
  - bounds preflight --ci
