version: "1"
project: bounds
languages: [python]
enforce: "off"
# Root-level bootstrap files (globs) — exempt from `validate --fail-on-unowned`.
entry_points: []
subsystems:
  - models
  - manifest
  - extract
  - cache
  - validate
  - gitutil
  - output
  - cli
